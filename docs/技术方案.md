# OnTheWay 技术方案

> 个人效率与复盘桌面应用 —— 笔记 / 今日TODO / GOAL / 日历 一体化
>
> | | |
> |---|---|
> | 版本 | v2.0 |
> | 日期 | 2026-09-16 |
> | 状态 | **与代码同步**（以 `src/`、`src-tauri/` 现状为准；v1.0 是开工前的设计稿） |
> | 目标平台 | Windows 10+ / macOS 13+ / Linux（二等公民） |

> 与 v1.0 相比的主要偏离，先列在最前面，免得读旧文档的人带着错误预期看代码：
>
> | v1.0 设计 | 实际实现 | 原因 |
> |---|---|---|
> | Milkdown（ProseMirror）所见即所得 | **CodeMirror 6 + Typora 式「藏标记」装饰** | Milkdown 在 P6 实机接入后被换掉：AST ↔ Markdown 互转造成输入跳行、内容漂移；CodeMirror 文档始终是 Markdown 源文，没有往返损耗 |
> | TanStack Query + Router + Virtual | **只有 Zustand**，两个 store | 五个视图共用一个 `DocumentView`，没有路由；数据量级不需要虚拟列表；乐观更新直接写在 store 里 |
> | Temporal polyfill | `lib/date.ts` 基于 `Date` + ISO 字符串 | 只做本地日期运算，Temporal 是多余的 5KB+ |
> | 行动项 = 嵌在文档里的 task 实体 | **行动项已迁回 Markdown 的 `- [ ]`**（迁移 0003） | 两个编辑面（正文 + 独立行动项区块）让用户搞不清在编辑什么 |
> | 「今日TODO」是一篇特殊笔记 | **就是今天这一天的 `day_doc`**（迁移 0004） | 今日TODO 和日历里的今天本来就该是同一篇 |
> | GOAL 是带层级 / KR / 进度的目标实体 | **一个周期一篇 Markdown**（周 / 月 / 年） | 先把「写目标」做顺，KR / 进度环等表结构保留但未接 UI |
> | 提醒调度器、每日自动备份、更新器 | 未实现 | 见 §16 |

---

## 目录

1. [产品范围与设计约束](#1-产品范围与设计约束)
2. [技术栈](#2-技术栈)
3. [架构总览](#3-架构总览)
4. [目录结构](#4-目录结构)
5. [数据层](#5-数据层)
6. [中文全文搜索](#6-中文全文搜索)
7. [IPC 契约](#7-ipc-契约)
8. [前端架构](#8-前端架构)
9. [设计系统](#9-设计系统)
10. [动效规范](#10-动效规范)
11. [编辑器（CodeMirror 6）](#11-编辑器codemirror-6)
12. [各模块现状](#12-各模块现状)
13. [性能](#13-性能)
14. [构建与发布](#14-构建与发布)
15. [测试](#15-测试)
16. [尚未实现 / 已实现未接入](#16-尚未实现--已实现未接入)
17. [风险登记](#17-风险登记)

---

## 1. 产品范围与设计约束

### 1.1 核心命题：一切皆文档

笔记、今日TODO、GOAL、日历的某一天、归档项 —— 全部是**同一种结构**：

```
（可选横幅）大标题 +（可选分段控件）
正文 Markdown（所见即所得编辑）
右侧目录树 + 底部状态栏
```

五个模块不是五套 UI，是同一个 `DocumentView` 的不同数据源。新增内容类型 = 在 `data/adapter.ts` 里多映射一个 `DocumentModel`，**不写新视图组件**。这是整个前端最重要的约束，守住它。

数据链上四种实体的关系（现状）：

```
day_doc(某一天) ──就是──> 今日TODO
goal(某周期)   ──周/月/年──> 日历右侧分段
note ──[[双链]]──> link 表 ──> note / task / goal
task ──due_date──> 日历当天「当日安排」
activity（append-only 日志，写入中，尚无读侧）
```

### 1.2 硬性约束

| 约束 | 说明 |
|---|---|
| 本地优先 | 所有数据落在本机 SQLite，无网络也 100% 可用 |
| Markdown 为唯一文本格式 | 笔记正文、某天的备注、目标全文都是 Markdown 字符串；**没有富文本 AST 落库** |
| 动效密度高但克制 | 参考 Telegram：短（80–320ms）、可打断、只跑合成器 |
| 中文优先 | jieba 分词搜索、中文排版、IME 输入不能卡顿吞字 |
| 单人可维护 | 不引入需要专职维护的基础设施 |

### 1.3 明确不做（v1）

- 多设备同步、协作、分享
- 移动端（数据模型已预留：UUIDv7、软删除、`updated_at`）
- AI 功能
- 「扩展」目前只有目录页和本地导入，**没有运行时**，见 §12.6

---

## 2. 技术栈

### 2.1 依赖清单（与 lock 文件一致）

**Rust 侧** — `src-tauri/Cargo.toml`

```toml
[features]
default = ["desktop-runtime"]
desktop-runtime = ["dep:tauri", "dep:tauri-specta", "dep:tauri-plugin-opener",
                   "dep:tauri-plugin-window-state", "dep:tauri-plugin-single-instance", ...]
typegen = ["dep:tauri", "dep:tauri-specta", "tauri/test",    # 只为导出 TS 绑定；capabilities 引用了
           "dep:tauri-plugin-opener", "dep:tauri-plugin-window-state"]  # 这两个插件的权限

[dependencies]
# protocol-asset 必须写在这里：tauri-build 只核对依赖行的 features
tauri = { version = "2", default-features = false, features = ["protocol-asset"], optional = true }
tauri-plugin-opener        = "2"   # optional
tauri-plugin-window-state  = "2"   # optional
tauri-plugin-single-instance = "2" # optional，桌面平台

rusqlite   = { version = "0.32", features = ["bundled", "backup", "chrono", "uuid"] }
r2d2       = "0.8"
r2d2_sqlite = "0.25"
jieba-rs   = "0.7"
rrule      = "0.14"
chrono     = { version = "0.4", features = ["serde"] }
chrono-tz  = "0.10"
uuid       = { version = "1", features = ["v7", "serde"] }
serde / serde_json / thiserror = "2" / once_cell

specta           = "=2.0.0-rc.22"     # 三个版本号锁死，RC 之间不兼容
specta-typescript = "=0.0.9"
tauri-specta     = "=2.0.0-rc.21"
```

`domain/` 和 `db/` **不依赖 tauri**。`--no-default-features` 下整个 lib 仍能编译并跑单测（§15）。

**前端** — `package.json`

```jsonc
{
  "dependencies": {
    "react": "^19", "react-dom": "^19",
    "@tauri-apps/api": "^2.11",
    "zustand": "^5",
    "motion": "^12",

    "@codemirror/state": "^6.7", "@codemirror/view": "^6.43",
    "@codemirror/commands": "^6.11", "@codemirror/language": "^6.12",
    "@codemirror/lang-markdown": "^6.5", "@codemirror/search": "^6.7",
    "@codemirror/language-data": "^6.5", "@lezer/highlight": "^1.2",   // 围栏代码按语言高亮
    "@tauri-apps/plugin-opener": "^2.5",                               // Mod+点击打开外链

    "@radix-ui/react-dialog": "^1", "@radix-ui/react-dropdown-menu": "^2",
    "@radix-ui/react-popover": "^1", "@radix-ui/react-tooltip": "^1",
    "lucide-react": "^0.475",
    "@dnd-kit/core": "^6", "@dnd-kit/sortable": "^10",   // 装了，尚未用上
    "fractional-indexing": "^3",                          // 装了，尚未用上
    "marked": "^15",                                      // 装了，尚未用上
    "clsx": "^2", "tailwind-merge": "^3"
  },
  "devDependencies": {
    "vite": "^6.2", "@vitejs/plugin-react": "^4",
    "typescript": "^5.7",
    "tailwindcss": "^4", "@tailwindcss/vite": "^4",
    "@biomejs/biome": "^1.9",
    "vitest": "^4", "happy-dom": "^20",
    "@tauri-apps/cli": "^2.11"
  }
}
```

### 2.2 选型理由（一句话版）

| 选择 | 理由 | 放弃的替代 |
|---|---|---|
| Tauri 2 | 安装包小、常驻内存低；Windows 上 WebView2 = Chromium，动画性能与 Electron 等价 | Electron、Flutter |
| React 19 | Motion / Radix / CodeMirror 绑定最完整 | Solid、Svelte |
| Motion | layout 动画 + spring 物理 + 可打断 | GSAP、纯 CSS |
| Tailwind v4 | CSS-first，design token 就是 CSS 变量，动效 token 与样式 token 同源 | CSS Modules |
| rusqlite 直连 | FTS5 / 自定义分词 / PRAGMA 的完全控制权 | tauri-plugin-sql |
| **CodeMirror 6** | 文档永远是 Markdown 源文；装饰层只做「藏标记 + 加样式」，光标进入范围标记原位出现，**不存在 AST 互转** | Milkdown（已试过并移除）、Tiptap |
| Zustand（仅此一个状态库） | 两个 store（UI / 数据）够用；没有路由、没有服务端缓存失效问题 | TanStack Query / Router |

---

## 3. 架构总览

### 3.1 进程与线程

```
┌─────────────────────────────────────────────────────────┐
│ WebView 进程 (WebView2 / WKWebView)                      │
│  主线程：React + Motion + CodeMirror + 两个 Zustand store  │
│  合成线程：transform / opacity 动画                       │
└───────────────────────┬─────────────────────────────────┘
                        │ Tauri IPC（tauri-specta 生成的类型安全调用）
┌───────────────────────┴─────────────────────────────────┐
│ Rust 主进程                                              │
│  #[tauri::command] → spawn_blocking → r2d2 池（6 连接）   │
│  阻塞线程池：SQLite（WAL）、jieba 分词、rrule 展开        │
│  setup：建库 + 迁移 + 首次播种 + jieba 后台预热           │
└─────────────────────────────────────────────────────────┘
```

没有 Web Worker，没有后台定时任务。**所有可能超过 1ms 的计算都在 Rust**（分词、搜索、rrule、摘要 / 字数派生）。

### 3.2 数据流

```
用户操作
   │
   ├─► useApp（UI 状态：工作区、选中项、主题、专注模式）—— 同步
   │
   └─► useData（数据状态：notes / goals / dayDocs / tasks）
          │
          ├─► 乐观更新 state ──► UI 立即变化 + 动画
          ├─► backend().xxx() ──► Tauri IPC ──► Rust ──► SQLite（+ activity）
          ├─► 成功：用返回值覆盖乐观值
          └─► 失败：回滚 + 写 error / saveError 让状态栏显示
```

`backend()` 在 Tauri 里走 IPC，在浏览器（`pnpm dev` 直开 1420）走 `data/mock.ts`（localStorage 持久化），语义尽量贴近 Rust 实现。视图和 store 只认 `Backend` 接口。

### 3.3 启动序列（`src/main.tsx`）

1. `initPreferences()`：把主题 / 减少动效落到 `<html>`（`index.html` 里的内联脚本已在首帧前落过一次，防暗色闪白）
2. **并行**：`preloadEditor()`（编辑器分包）+ `useData.initialize()`（笔记全文 + 归档 + 标记日期）
3. `installCloseGuard()`：接管 Tauri `onCloseRequested`，关窗前 flush 所有编辑器
4. `createRoot().render(<Shell/>)`
5. 双 `requestAnimationFrame` 后 `signalReady()` → Rust `window.show()`

窗口在 `tauri.conf.json` 里是 `visible: false`，所以第 2 步多等的时间用户看不到，换来首帧就是编辑器而不是占位预览。

---

## 4. 目录结构

```
OnTheWay/
├── docs/
│   ├── 技术方案.md               ← 本文档
│   └── TODO.md                   ← 进度、决策记录、坑
├── Prototype/                    # 原型截图，色板和布局的 1:1 来源
│
├── src/
│   ├── main.tsx                  # 启动序列（§3.3）
│   ├── app/
│   │   ├── Shell.tsx             # 三栏骨架、工作区切换动画、专注模式
│   │   └── store.ts              # useApp：UI 状态 + 主题 + 零点翻页
│   ├── data/
│   │   ├── types.ts              # 领域类型（窄联合）+ DocumentModel
│   │   ├── backend.ts            # Backend 接口；Tauri IPC / 浏览器 mock 切换
│   │   ├── store.ts              # useData：数据缓存 + 乐观更新 + 保存
│   │   ├── adapter.ts            # ★ 工作区 + 选中项 → DocumentModel
│   │   ├── mock.ts               # 浏览器 mock 后端（localStorage）
│   │   └── seed.ts               # mock 的示例数据
│   ├── editor/
│   │   ├── MarkdownEditor.tsx    # CodeMirror 实例、两层装饰、widget
│   │   ├── markdownStyleRegistry.ts  # 语法 → 类名 / widget 的注册表
│   │   ├── markdownKeymap.ts     # Typora 对齐的快捷键
│   │   ├── markdownCommands.ts   # 加粗 / 标题 / 列表 / 清除格式等命令
│   │   ├── markdownTable.ts      # GFM 表格解析（widget 和占位预览共用）
│   │   ├── animatedEmojiSet.ts   # 动态表情设计稿：36 个 SVG（六组）+ 每个部件的关键帧
│   │   ├── animatedEmoji.ts      # 动态表情的模板 / 播放器 / 看得见才循环（不依赖 CodeMirror）
│   │   ├── EmojiPicker.tsx       # Mod-E 动态表情选择器
│   │   ├── debouncedSave.ts      # 串行 400ms 防抖保存器
│   │   ├── searchPanel.ts        # Mod-F 查找 / 替换面板
│   │   └── saveBus.ts            # 全局 flush 注册 + 关窗保护
│   ├── components/
│   │   ├── DocumentView.tsx      # ★ 统一文档视图（标题、编辑器、目录、状态栏）
│   │   ├── Sidebar.tsx / TitleBar.tsx / CommandPalette.tsx
│   │   ├── Outline.tsx           # 右侧目录树（跟编辑器行号联动）
│   │   ├── OverlayScrollbar.tsx  # 自绘覆盖式滚动条
│   │   ├── ActionItem.tsx        # 日历「当日安排」的任务行 + 勾选动画
│   │   ├── ListColumn.tsx / RowMenu.tsx / SearchInput.tsx / Segmented.tsx
│   │   └── Logo.tsx / NoteIcon.tsx / ThemeToggle.tsx
│   ├── views/                    # 只有列表栏是各自的；正文统一走 DocumentView
│   │   ├── NotesView.tsx         # 笔记列表：标题筛选、三种排序、置顶分组
│   │   ├── CalendarView.tsx      # 月历面板
│   │   ├── ArchiveView.tsx       # 归档列表
│   │   └── ExtensionsView.tsx    # 扩展目录页（整页，不走 DocumentView）
│   ├── extensions/catalog.ts     # 扩展清单校验 + 本地导入（localStorage）
│   ├── lib/
│   │   ├── bindings.ts           # ★ tauri-specta 生成，勿手改（biome 已忽略）
│   │   ├── date.ts               # 全局唯一时间入口：ISO 日期、周期、中文格式化
│   │   ├── markdown.tsx          # buildOutline；renderMarkdown 仅占位用
│   │   ├── plainText.ts          # 摘要 / 字数的纯文本（mock 用，与 Rust search.rs 同规则）
│   │   ├── emojiText.ts          # 动态表情短码 → Unicode（列表摘要、目录；主包里只放这张小表）
│   │   ├── motion.ts             # spring / tween / stagger / layoutId 命名空间
│   │   ├── tauri.ts              # isTauri、窗口控制、ready 信号
│   │   ├── platform.ts           # mac / Windows 快捷键显示
│   │   └── cn.ts
│   └── styles/globals.css        # @theme token、暗色、prose、编辑器全部样式
│
├── src-tauri/
│   ├── Cargo.toml                # feature：desktop-runtime / typegen
│   ├── tauri.conf.json
│   ├── capabilities/default.json
│   ├── examples/export_bindings.rs   # cargo run --example export_bindings --features typegen
│   ├── migrations/
│   │   ├── 0001_init.sql
│   │   ├── 0002_unify_markdown_documents.sql
│   │   ├── 0003_remove_legacy_action_groups.sql
│   │   └── 0004_day_doc_title.sql
│   └── src/
│       ├── main.rs / lib.rs      # 窗口命令、插件、setup、命令注册
│       ├── state.rs              # AppState { pool, db_path }
│       ├── error.rs              # AppError（tag = kind，跨 IPC）
│       ├── commands.rs           # 薄封装：校验 + spawn_blocking
│       ├── db/{mod,migrate,pragma}.rs
│       └── domain/               # 纯逻辑，不依赖 tauri
│           ├── model.rs          # 跨 IPC 的领域类型
│           ├── note.rs           # 笔记 CRUD、双链同步、搜索
│           ├── goal.rs           # 周期目标 + 某天文档 + 标记日期
│           ├── task.rs           # 任务查询 / 勾选
│           ├── search.rs         # jieba 分词、摘要、字数
│           ├── event.rs          # rrule 展开、时区转换（已实现，未接命令）
│           ├── activity.rs       # append-only 日志
│           └── seed.rs           # 首次启动示例内容
├── biome.json / tsconfig.json / vite.config.ts / pnpm-workspace.yaml
└── .claude/launch.json           # 浏览器预览：pnpm dev @ 1420
```

**分层原则**：`commands.rs` 只做参数校验（日期格式、空内容）和 `spawn_blocking`；业务在 `domain/`；`domain/` 用 `:memory:` SQLite 直接跑单测。

---

## 5. 数据层

### 5.1 存储决策

**SQLite 是唯一真相来源**，所有正文以 Markdown 字符串存在字段里。

为将来同步预留的四条（已做到）：UUIDv7 主键（`db::new_id()`）、软删除 `deleted_at`、每表 `updated_at`（UTC ms）、不用 `AUTOINCREMENT`（`activity` 除外）。

数据库路径：`app.path().app_data_dir()/ontheway.db`。首次启动 `domain::seed::ensure()` 写入示例内容并在 `setting` 表落 `seeded_v1` 标记 —— 用户删光了也不会再塞回来。

### 5.2 时间与时区约定

| 场景 | 存储 | 类型 |
|---|---|---|
| 记录型时间戳（created_at / updated_at / completed_at / archived_at） | UTC 毫秒 | `INTEGER` |
| 定时事件、提醒（`event` 表，未接 UI） | UTC 毫秒 + IANA `tz` | `INTEGER` + `TEXT` |
| 任务截止日、某一天的文档、周期起点 | 本地日期 `'YYYY-MM-DD'` | `TEXT` |

前端所有日期运算经过 `src/lib/date.ts`（`ISODate` 字符串 + 少量 `Date` 封装）：`periodStartOf(horizon, date)` 算周一 / 1 号 / 1 月 1 日；`isoWeekNumber`；`monthGrid` 固定 6×7；中文格式化函数一组。**业务代码里不直接 `new Date()` 做加减。**

Rust 侧 `domain/event.rs` 提供 `local_to_utc_ms`（DST 回拨取较早、跳时明确报错）和 `expand_rrule`（先约束到查询窗口再限量，应用 EXDATE 与 override）。

### 5.3 表结构（迁移后的现状）

`0001_init.sql` 建全部表，`0002`–`0004` 三次改动。当前有效结构：

```sql
-- ===== 笔记 =====
note (id, title, content_md, content_tokens, excerpt, icon, word_count,
      is_pinned, is_archived, archive_category, archived_at,
      action_title /*0003 后恒为 NULL*/, sort_key, created_at, updated_at, deleted_at)
note_fts  -- FTS5 外部内容表：title + content_tokens，unicode61；三个触发器同步

-- ===== 目标：一个周期一篇（0002 重建） =====
goal (id, title, horizon CHECK IN ('week','month','year'), period_start,
      content_md, action_title /*恒 NULL*/, created_at, updated_at, deleted_at,
      UNIQUE (horizon, period_start))

-- ===== 某一天的文档（0004 加 title） =====
day_doc (date PRIMARY KEY, title, note_md, created_at, updated_at)

-- ===== 任务 =====
task (id, title, notes_md, status, meta, priority, is_all_day, due_date, due_at,
      time_label, category, remind_at, rrule, completed_at, goal_id, parent_id,
      sort_key, estimate_min, actual_min, created_at, updated_at, deleted_at)

-- ===== 链接：多态双向 =====
link (id, src_type, src_id, dst_type, dst_id, kind /*ref|action|...*/, sort_key, created_at,
      UNIQUE (src_type, src_id, dst_type, dst_id, kind))

-- ===== 行为日志 =====
activity (id AUTOINCREMENT, at, local_date, entity_type, entity_id, action, payload)

-- ===== 建了表、写了迁移、目前没有命令读写 =====
key_result, calendar, event, event_override, tag, entity_tag, review, setting(仅 seeded 标记)
```

三次迁移各自解决的问题：

| 迁移 | 做了什么 | 为什么 |
|---|---|---|
| `0002_unify_markdown_documents` | `goal` 的 `description_md` + `after_md` 合并成 `content_md`，重建表加 `UNIQUE(horizon, period_start)` | 一篇文档被拆成两段导致编辑器和展示层双轨 |
| `0003_remove_legacy_action_groups` | 把 `link(kind='action')` 挂着的任务写回宿主正文的 `## 行动项标题` + `- [ ]` 列表，删掉 link 和孤儿 task，`action_title` 置 NULL | 整篇文档只能有一个编辑面 |
| `0004_day_doc_title` | `day_doc` 加 `title`；把旧的特殊笔记 `n-today` 折进它最后编辑那天的 `day_doc` 并软删 | 今日TODO 就是今天这一天 |

`link(kind='ref')` 现在是唯一在用的链接种类：`note::upsert` 在同一事务里重扫正文的 `[[标题]]`，能解析到的（note > task > goal 优先级按标题匹配）写入 link，解析不到的保留在 Markdown 里下次再试。

### 5.4 迁移策略

`PRAGMA user_version` + `include_str!` 数组，不引第三方框架（`db/migrate.rs`）：

- 版本号 = 数组下标 + 1；只能追加，**已发布条目永不修改**
- 每条迁移一个事务，失败回滚
- `current > target` → `AppError::DbTooNew`，拒绝启动（用户装新版又退回旧版的场景）
- `current > 0` 时先 Online Backup 到 `backups/pre-migration-v{n}.db`

### 5.5 连接与并发

`db/pragma.rs::configure` 通过 r2d2 的 `with_init` 在**每个连接**上执行：WAL、`synchronous=NORMAL`、`foreign_keys=ON`（连接级开关，漏设会静默失去约束）、`busy_timeout=5000`、`temp_store=MEMORY`、`mmap_size=256MB`。池大小 6。

命令层统一走 `with_db(state, |conn| ...)`：`spawn_blocking` 拿连接执行，rusqlite 是同步阻塞的，不能占 tokio worker。

### 5.6 派生列

`note::upsert` 统一计算 `content_tokens`（jieba）、`excerpt`（纯文本前 60 字 + `…`）、`word_count`（中文按字、英文按词），调用方不用管，三者永远和 `content_md` 一致。

---

## 6. 中文全文搜索

### 6.1 为什么不能直接用 FTS5

`unicode61` 不切中文（整句一个 token）；`trigram` 按 3 字符切，中文双字词（「笔记」「目标」）直接搜不到。

### 6.2 方案：jieba 预分词 + unicode61（`domain/search.rs`）

- 写入：`tokenize_for_index` 用 `cut_for_search` 产生**重叠细粒度切分**（「季度目标」→ `季度` `目标` `季度目标`），过滤掉不含字母数字汉字的标点 token，空格拼接存进 `content_tokens`
- 查询：`build_match_query` 同样切词，每个 token 双引号包裹 + 转义 + 前缀 `*`（单字「笔」能命中「笔记」），隐式 AND；纯标点 / 空查询返回 `None` → 命令直接返回空结果而不是全表
- 高亮：**不用** SQLite `snippet()`（作用在分词串上，中文会显示成「今天 开会 讨论」）。`query_tokens` 把 token 按长度降序去重传回前端，由前端在原始正文上高亮
- `JIEBA` 是 `once_cell::Lazy`，首次初始化 ~50ms，`setup` 里 `std::thread::spawn(search::warm_up)` 预热

### 6.3 查询 SQL

```sql
SELECT n.id, n.title, n.excerpt, n.icon, n.is_archived, n.updated_at,
       bm25(note_fts, 10.0, 1.0) AS score      -- 标题权重 10x
FROM note_fts JOIN note n ON n.rowid = note_fts.rowid
WHERE note_fts MATCH ?1 AND n.deleted_at IS NULL
ORDER BY score LIMIT ?2;                       -- bm25 越负越相关
```

`search_notes` 命令、`Backend.searchNotes`、`useData.searchNotes` 都在，**但当前没有任何界面调用它**：笔记列表改成只按标题本地筛选（原因见 `NotesView.tsx` 注释 —— 全文匹配的结果和标题栏对不上，且异步回填会闪一次），命令面板的「笔记」组也是本地过滤。全文搜索的入口待做。

---

## 7. IPC 契约

### 7.1 类型生成：tauri-specta

三个 crate 版本锁死（`=2.0.0-rc.22` / `=0.0.9` / `=2.0.0-rc.21`），RC 之间不兼容。

生成时机有两条：

1. **debug 启动时**：`lib.rs::run()` 在 `#[cfg(debug_assertions)]` 下写 `../src/lib/bindings.ts`
2. **不起 app**：`cargo run --example export_bindings --features typegen --no-default-features`，用 `tauri::test::MockRuntime` 收集同一份命令列表

导出配置：`bigint(Number)`（时间戳远低于 `MAX_SAFE_INTEGER`）、文件头加 `// @ts-nocheck`（无事件时生成的辅助代码会触发 `noUnusedLocals`）。`biome.json` 忽略该文件。

生成物的形状：

```ts
export const commands = {
  async noteList(archived: boolean): Promise<Result<NoteSummary[], AppError>> { ... },
  async ready(): Promise<void> { ... },   // 窗口命令没有 Result 包装
  ...
};
export type AppError =
  | { kind: "Db"; message: string } | { kind: "NotFound"; message: string }
  | { kind: "Invalid"; message: string } | { kind: "BadRrule"; message: string }
  | { kind: "DbTooNew"; message: { found: number; supported: number } }
  | { kind: "Io"; message: string } | { kind: "Internal"; message: string };
```

`data/backend.ts` 里 `unwrap()` 把 `Result` 拆开：`status === "error"` 就 `throw result.error`，store 的 `messageOf()` 再把 `{kind, message}` 拆成人能读的字符串。

### 7.2 命令清单（`lib.rs::command_builder`）

| 域 | 命令 | 说明 |
|---|---|---|
| **窗口** | `ready` | 首帧后 `show() + set_focus()` |
| | `win_minimize` / `win_toggle_maximize` / `win_is_maximized` / `win_start_dragging` | 无边框窗口自定义标题栏用 |
| | `win_close` | `close()`，触发前端 `onCloseRequested` 保存 |
| | `win_force_close` | `destroy()`，只由关窗保护在 flush 后调用 |
| **note** | `note_list(archived)` | 摘要列表，不含正文；置顶优先，按 updated_at / archived_at 降序 |
| | `note_get(id)` | 全文 |
| | `note_upsert(input)` | `id` 为 null 即新建；标题正文不能同时为空；派生列 + 双链同步 + activity 同事务 |
| | `note_set_pinned(id, pinned)` / `note_archive(id, category?)` / `note_restore(id)` / `note_delete(id)` | 归档会清掉置顶；删除是软删 |
| **search** | `search_notes(query, limit≤200)` | §6 |
| **task** | `task_toggle(id)` | todo ↔ done，记 activity `completed` / `reopened` |
| **goal** | `goal_get(horizon, period_start)` | 没写过返回空文档（id 空、updated_at 0），不落库 |
| | `goal_save(horizon, period_start, content_md)` | `ON CONFLICT(horizon, period_start)` upsert，软删过的复活 |
| **calendar** | `calendar_day(date, carry_over)` | 当天任务 + `day_doc`；`carry_over` 只在请求「今天」时传 true |
| | `calendar_day_save(date, title, note_md)` | upsert |
| | `calendar_marked(from, to)` | 有任务或写过东西的日期（日历上标墨点） |
| **system** | `db_stats()` | 各表计数、库大小、路径 |

所有非窗口命令的 `String` 日期参数都先过 `validate_date`（严格 `YYYY-MM-DD`），`goal_*` 再由 domain 校验 `period_start` 真的是周期起点，免得同一周被两个键各存一份。

### 7.3 「延续」语义（`goal::day_doc`）

请求「今天」而今天还没写过时，把之前最近写过的一天（标题 + 正文）延续过来，`carried_from` 指向那天。**延续的内容不落库**，用户一编辑才以今天的身份 `calendar_day_save`；昨天那篇原样留在昨天。翻到昨天之前的空白日期就是空白。前端 `forgetCarriedDays()` 在跨过零点时丢掉缓存里所有延续来的文档。

---

## 8. 前端架构

### 8.1 两个 store

| store | 文件 | 内容 |
|---|---|---|
| `useApp` | `app/store.ts` | 工作区与切换方向、各区选中项、`todayDate`（零点翻页信号）、日历分段 / GOAL 尺度、搜索框文本、主题、减少动效、命令面板开关、专注模式 |
| `useData` | `data/store.ts` | `notes` / `archived`（含全文）、`tasks`（按 id 索引）、`goals`（按 `horizon:periodStart`）、`dayDocs`、`markedDates`、加载 / 保存中 / 错误状态 |

**编辑器内容不进 React state。** CodeMirror 持有文档，`updateListener` 里防抖 400ms 才调 `onSave`；目录树用的实时正文另走一条 250ms 防抖，且只在标题集合真的变化时才 `setState`。

### 8.2 `useData` 的加载策略

- `initialize()`：并行取 `noteList(false)` / `noteList(true)` / `calendarMarked(2000..2100)`，再对每篇 `noteGet` 取全文。**不取目标和某天** —— 切到哪个周期 / 哪一天再 `loadGoal` / `loadDay`，已缓存的不重复取
- `Shell` 里两个 effect 负责按需加载：今天这一篇一进应用就取（今日TODO 用）；日历 / GOAL 按当前分段和选中日期取
- 保存：`saveDocument(target, contentMd)` 三种 `DocumentSaveTarget`（note / goal / day）各自判重后写库。**未加载完不写**（`source` 为空直接返回），否则空编辑器一输入会把已有内容整篇覆盖 —— 这是修过的 P0

### 8.3 乐观更新（示例：`toggleTask`）

```ts
const optimistic = { ...current, status: done ? "todo" : "done", completedAt: ... };
set((state) => patchTask(state, id, optimistic));      // notes / goals / dayDocs 里同步替换
try {
  const saved = await (await backend()).taskToggle(id);
  set((state) => patchTask(state, id, saved));
} catch (error) {
  set((state) => ({ ...patchTask(state, id, current), error: messageOf(error) }));
}
```

`togglePin` / `archiveNote` / `restoreNote` / `deleteNote` 同款。`createNote` 例外：id 由后端分配，等一个来回。

自动保存**刻意不重排列表**：每 400ms 刷一次 `updatedAt`，按它排序会让正在编辑的笔记当着用户的面往上跳；顺序在下次加载 / 置顶 / 归档时自然更新。

### 8.4 统一文档模型

```ts
interface DocumentModel {
  key: string;                       // React key + 滚动位置
  title: string;
  banner?: { icon: "archive"; text: string };
  segments?: { group; options; active };   // 标题右侧分段（GOAL 周月年 / 日历四段）
  bodyMd: string;
  actionGroup?: { title; tasks; hideHeader? };   // 现在只有日历「当日安排」用
  eyebrow?: string;                  // 「今天 · 9月16日 周三」
  statusParts: string[];
  deletable?: boolean;
  editor?: { target: DocumentSaveTarget; titleEditable?: boolean };  // 没有 = 只读占位
}
```

`data/adapter.ts::useCurrentDocument()` 是唯一的映射点：

| 工作区 | 数据源 | 标题可编辑 |
|---|---|---|
| 笔记 | `notes[selectedNoteId]`，找不到退到第一篇 | 是 |
| 今日TODO | `dayDocs[todayDate]` | 是 |
| /GOAL | `goals[goalHorizon : periodStartOf(today)]` | 否，由周期算 |
| 日历 · 日TODO | `dayDocs[selectedDate]` | 是 |
| 日历 · 周/月/年 GOAL | `goals[scope : periodStartOf(selectedDate)]` | 否 |
| 归档 | `archived[selectedArchiveId]`，带横幅 | 是 |
| 扩展 | 不走 DocumentView | — |

数据还没回来时 `editor` 为 `undefined`，`DocumentView` 渲染 `DocumentPreview`（`renderMarkdown`）作为占位 —— 此时 `bodyMd` 通常是空串。**这不是一个「只读模式」**，`renderMarkdown` 现在只服务于占位和空状态文案。

### 8.5 `DocumentView` 的职责

标题（可编辑时是 `EditableDocumentTitle`）、分段控件、编辑器或占位、日历的 `ActionGroup`、右侧 `Outline`、底部状态栏（`statusParts` + 保存中 + `saveError`）、右下删除按钮、专注模式按钮（仅笔记区）。编辑器分包在模块求值时就开始 `import()`，`preloadEditor()` 让 bootstrap 能 await 它。

### 8.6 保存链路

```
CodeMirror updateListener ──► createDebouncedSaver(save, 400)
                                   │ schedule()：重置计时
                                   │ flush()：串行写链，失败版本放回队列
                                   ▼
                              useData.saveDocument()
                                   │
registerEditorFlush(saver.flush) ──► saveBus.flushAllEditors()
       触发点：Mod-S、失焦、切换文档（卸载）、Tauri onCloseRequested
```

关窗保护（`saveBus.installCloseGuard`）：`preventDefault` → `flushAllEditors()` → `win_force_close`。保存失败时**不锁死窗口**：把原因写进 `saveError`，并放行下一次关闭请求（再点一次 = 放弃未保存内容）。浏览器里退化为 `beforeunload`。

### 8.7 零点翻页

`startTodayTicker()`：`setTimeout` 对准下一个 00:00:00.500，醒来（`visibilitychange`）再对一次表。`todayDate` 变了就更新，日历选中若停在旧的今天则一起带过去。

### 8.8 专注模式（zen）

只在笔记区。左侧 chrome（导航 240 + 列表 300 = `RAIL_WIDTH` 540）切成 `absolute` 后用 `x: -540` 的 tween（460ms）滑出；正文画布往左多铺 540px 负外边距，让进出两个方向都是「chrome 在白色画布上滑动」。Esc 退出；离开笔记区自动退出。刻意**不**动画化 width / marginLeft —— 开了 lineWrapping 的 CodeMirror 会逐帧重新折行。

---

## 9. 设计系统

### 9.1 Design token（`styles/globals.css` 的 `@theme`）

亮色直接取样自 `Prototype/`，1:1；暗色手工配平，保持同样的暖中性调性。用 hex 而不是 OKLCH（原型是 hex，取样比生成更准）。

| token | 亮 | 暗 | 用途 |
|---|---|---|---|
| `ink` / `body` | `#1A1A18` / `#353532` | `#EEEEE9` / `#D2D2CB` | 标题 / 正文 |
| `muted` / `faint` | `#74746E` / `#A3A39B` | `#8D8D85` / `#61615B` | 次级 / 最弱 |
| `canvas` / `panel` / `rail` | `#FFFFFF` / `#FAFAF8` / `#F1F1ED` | `#1E1E1C` / `#1A1A18` / `#141413` | 主内容 / 列表栏 / 导航 |
| `raised` | `#E5E5DF` | `#2B2B28` | 导航选中、hover 实心 |
| `line` / `line-strong` | `#F1F1EE` / `#E7E7E1` | `#2A2A27` / `#35352F` | 分隔线 |
| `accent` / `accent-hover` / `accent-ink` | `#2F63D8` / `#2A58C2` / `#FFF` | `#6B93F0` / `#82A4F4` / `#10182B` | 主色 |
| `accent-wash` / `accent-wash-2` / `accent-line` | `#F1F4FA` / `#F3F6FC` / `#DBE4F7` | `#1C2437` / `#1A2133` / `#2C3A57` | 选中底、周行底、浅边 |
| `success` / `danger` / `warning` | `#2F8A5B` / `#C8483D` / `#B8802A` | `#58B587` / `#E0776B` / `#D5A355` | |

圆角 `xs 4 / sm 6 / md 8 / lg 10 / xl 14 / 2xl 18`；阴影三档 `card / float / modal`，只用于浮层；动效 token `instant 80 / fast 140 / base 220 / slow 320ms` 与三条缓动曲线。

### 9.2 暗色机制

`<html data-theme="light|dark">` **始终有值**：`index.html` 内联脚本在首帧前从 `localStorage.otw.theme` 或系统偏好落一次，`applyTheme()` 之后维护。Tailwind 的 `dark:` 变体通过 `@custom-variant dark (&:where([data-theme="dark"], [data-theme="dark"] *))` 绑到这个属性 —— 只靠 `prefers-color-scheme` 的话变体不生效。`@media (prefers-color-scheme: dark)` 块用 `:root:not([data-theme="light"])` 兜底。

### 9.3 布局

| 区域 | 宽度 | 出现条件 |
|---|---|---|
| 标题栏 | 38px，`fixed`，无背景，按钮平时 opacity 0.28 | 仅 Tauri 内 |
| 左导航 | 240px | 常驻 |
| 中列表栏 | 300px | 笔记 / 日历 / 归档；今日TODO / GOAL / 扩展 是两栏 |
| 主内容 | flex-1，正文 max-w 860px | 常驻 |
| 右目录树 | 180px | `xl:`（≥1280px）；条目多了自己滚动，并跟着当前小节走；专注模式下变成贴右缘的浮层刻度（标题多了刻度等比挤紧，整列留在屏幕内） |

### 9.4 字体与中文排版

系统字体栈优先（Inter → Segoe UI Variable → PingFang SC → Microsoft YaHei UI → Noto Sans SC），零加载延迟。不从 Google Fonts 拉。编辑器正文 17px / 1.82 / `letter-spacing 0.012em`；标题 `letter-spacing -0.015em`。

### 9.5 组件基础

Radix 目前只用到 `DropdownMenu`（`RowMenu`），其余 dialog / popover / tooltip 装了没用；命令面板是自己用 Motion 写的浮层。样式全部自己写；不套 shadcn 默认外观。图标 lucide。滚动条是自绘的 `OverlayScrollbar`（原因：`scrollbar-width: none` 与 `::-webkit-scrollbar` 二选一；原生条在 Windows 占 10px 会让正文整体左移；且 CodeMirror 按视口渲染时 `scrollHeight` 会边滚边涨 25%，滑块要用指数缓动跟随而不是离散校正）。

---

## 10. 动效规范

### 10.1 三条铁律

1. **只动 `transform` 和 `opacity`。** 需要「宽度动画」时用 Motion 的 `layout` 或 transform 补偿
2. **绝不动画化 `backdrop-filter` 和 `box-shadow`。** 命令面板的毛玻璃是静态的，只动 opacity / scale
3. **时长 80–320ms，且可打断。** spring 天然可打断；确定时长的 tween 只用在「必须在某个点之前结束」的场景

### 10.2 预设（`lib/motion.ts`）

```ts
spring.snappy  { stiffness: 520, damping: 34, mass: 0.7 }  // 按钮、勾选
spring.smooth  { stiffness: 320, damping: 30 }             // 列表重排、指示器（默认）
spring.gentle  { stiffness: 190, damping: 26 }             // 面板滑入
spring.bouncy  { stiffness: 420, damping: 17 }             // 完成、达成（少用）
spring.layout  { stiffness: 380, damping: 34, mass: 0.9 }  // layout 重排专用

tween.instant / fast / base / slow = 80 / 140 / 220 / 320ms，ease-out-quint（slow 用 expo）
stagger(index, step = 22ms, cap = 10)
layoutIds = { navIndicator, listSelection, outlineIndicator, segmentThumb(group), calendarDay }
```

### 10.3 场景对照（现状）

| 场景 | 技术 | 参数 | 备注 |
|---|---|---|---|
| 工作区切换 | `AnimatePresence mode="popLayout" anchorX="right"` | 进场纯 opacity `tween.base`；退场 `x: dir * -12` + opacity `tween.fast` | **进场不带 transform**，编辑器才能在第一帧直接挂上（§11.5）。popLayout 把退场那屏冻结在原尺寸上交叉淡出，列表栏进出引起的收窄不会让它重新折行；`anchorX="right"` 是因为它的定位父级左边缘会挪 300px，钉右边才原地不动 |
| 列表栏出现 / 消失 | `x: -300 → 0`，整块从导航栏（z-20）底下抽出 | `tween.base`（220ms） | 挂载瞬间就以完整宽度进 flex 流，布局只变一次；之后纯 transform。以前动画化 `marginLeft`，220ms 里每帧重排、CodeMirror 逐帧折行，头几帧稳定 28ms 一帧 |
| 列表栏内容切换 | `AnimatePresence mode="wait"` | `tween.base` | key 只跟「有没有列表栏」走，笔记 → 日历不整条收起再展开 |
| 专注模式 | `x: -540`，chrome 层切 absolute | 460ms 自定义 ease-out | §8.8 |
| 导航活动项 / 列表选中 / 目录活动条 | `layoutId` | `spring.smooth` | |
| 标题切换 | `AnimatePresence popLayout` 上下滚动替换 | | |
| 标题下分隔线 | `scaleX 0→1` | 450ms | |
| 任务勾选 | SVG `pathLength` + 圆环 scale 脉冲 + 文字色 | `spring.snappy` | 三层同时，约 380ms |
| 命令面板 | 静态 backdrop-blur + opacity / scale | `popIn` variants | |
| 日历切月 | `AnimatePresence`，方向感知 | `tween.base` | 事件块只淡入，不 stagger |
| 主题切换按钮 | 图标旋转交叉淡入 | | |
| 悬停反馈 | 纯 CSS transition | 80ms | 极短的用 CSS |

### 10.4 减少动效

- `Shell` 上 `<MotionConfig reducedMotion={reduceMotion ? "always" : "user"}>`
- 应用内开关存 `localStorage.otw.prefs`，同时落 `<html data-reduce-motion>`（CSS 动画据此关闭，如 Logo 的笔画描边停在「写完」态）
- 组件里判断系统偏好用 `lib/motion.ts::usePrefersReducedMotion()`（直接问 `matchMedia`），**不要用** motion 自带的 `useReducedMotion()` —— `MotionConfig` 只要设过一次 `"always"`，它之后就一直返回 true，直到刷新页面

---

## 11. 编辑器（CodeMirror 6）

### 11.1 原理

文档**始终是 Markdown 源文**。装饰层对「光标不在的语法范围」隐藏标记（`Decoration.replace`）并施加排版样式（`Decoration.mark` / `Decoration.line`），光标一进入，标记在原位置重新出现。`#`、`**`、链接 URL 都能直接改，不存在富文本 AST 与 Markdown 互转导致的输入跳行。

解析器（`editor/markdownParser.ts`）：在 `commonmarkLanguage` 上自己拼 GFM + Superscript + Emoji，再加两个自定义 lezer 扩展 —— `$…$` / `$$…$$` 公式节点（必须在解析阶段整块认下来，否则 `$a*b$ 和 $c*d$` 里的 `*` 会配成强调）和单个 `~` 的删除线（按 GitHub 规矩，一个或两个波浪线都是删除线，不再是 Pandoc 下标；下标改走 `<sub>`）。`codeLanguages: languages`（`@codemirror/language-data`）让围栏代码按语言名懒加载对应解析器；配色由 `editor/codeHighlight.ts` 的 `HighlightStyle` 发类名、`globals.css` 的 `--code-*` 变量上色，只覆盖代码里的 tag，Markdown 自己的 tag（heading / emphasis / processingInstruction…）刻意不碰，免得和装饰层打架。

### 11.2 两层装饰（`MarkdownEditor.tsx`）

拆成两层是因为成本量级完全不同：

| 层 | 类型 | 负责 | 遍历范围 |
|---|---|---|---|
| 块级 | `StateField` | 折叠围栏行 / Setext 下划线行 / 分隔线行 / front matter 围栏行 / `[TOC]` 行，把表格、`$$` 公式块、块级 HTML、HTML 注释块、```mermaid / ```csv 围栏、收起的 callout 正文换成 widget —— 这些改变行结构，CodeMirror 要求必须来自 StateField；顺带收集引用标签、脚注正文（悬停预览用）、`*[缩写]: 全称` | 全文，但只下探 `Document / BulletList / OrderedList / ListItem / Blockquote`，`Paragraph` 只扫行不下探 |
| 行内 | `ViewPlugin` | 标记隐藏、加粗斜体、任务勾选框、列表符号（自动编号 / 按深度换符号）、语言名标签、图片、callout 徽章、脚注、双链、Emoji / 实体 / 硬换行替身、行内公式、行内 HTML 标签配对、`<br>` / `<img>` / 注释替身、定义列表、缩写提示、智能标点、`==` 源码规则 | 只处理 `view.visibleRanges`，成本与文档长度无关 |

拆分前一个 StateField 深度遍历全文，53KB 文档每次按键 5.3ms；拆分后 0.57ms。块级层给行内层一个 `foldedLines` 集合，被整行折叠的行不再渲染行内装饰。

`selectionTouchesRange` 决定「光标在不在这个范围里」：空选区看 `head`，非空选区看是否相交。

### 11.3 注册表（`markdownStyleRegistry.ts`）

新增纯样式语法只需在这里加一行：

- `markdownStyleRules`：语法树节点名 → 类名，`kind: "mark"`（行内）或 `"line"`（整行）
- `hiddenMarkerNodes`：需要藏起来的标记节点（`HeaderMark`、`EmphasisMark`、`CodeMark`、`QuoteMark`、`LinkMark`…）
- `widgetByNode`：需要 DOM 表现的（`TaskMarker` → 勾选框、`HorizontalRule`、`Image`、`Table`、`Emoji`、`Entity`、`HardBreak`、`InlineMath`、`HTMLTag`、`Comment`）
- `markdownSourceStyleRules`：CommonMark 树不会合并成单节点的自定义行内语法，目前只剩 `==…==`，源码级匹配；配对不能跨段落、不能落在代码字面量里，对称标记还要求内容非空且首尾不是空白，免得 `a == b == c` 被当成高亮。（`<u>` 以前也在这里，现在所有行内 HTML 标签统一走语法树里的 `HTMLTag` 节点配对。）

方括号家族（callout 标签、脚注、双链、`[TOC]`）不走注册表：CommonMark 把没有定义的 `[文字]` 一律解析成短引用 `Link`，得在行内层按内容分流 —— `[!标签]` 在引用块首行是 callout，`[^id]` 是脚注，外面再套一层方括号的是双链，都不是的话看块级层收集的 `referenceLabels`：有定义才当链接，否则就是普通的方括号文本（以前任何 `[x]` 都被染成链接色并藏掉括号）。

所有 widget 在 `editor/widgets.ts`（公式和图表的替身单独在 `editor/math.ts` / `editor/diagram.ts`，因为它们要懒加载 KaTeX / mermaid）；表格单元格和 `[TOC]` 条目里的行内格式由 `editor/inlineDom.ts` 渲染成真实 DOM（不走 innerHTML）；HTML 的标签白名单、样式白名单和块级净化重建在 `editor/html.ts`；Emoji 短码表在 `editor/emoji.ts`，实体表在 `editor/entities.ts`，callout 的标签分类与图标在 `editor/callout.ts`，front matter 识别在 `editor/frontMatter.ts`，链接目标解析与打开在 `editor/links.ts`。

**HTML 的安全边界**：用户写的字符串永远不进 innerHTML。行内标签只是映射成装饰类（`<b>` 和 `**` 同一个类，`<span style>` 只放行颜色 / 字号 / 粗细那几个属性）；块级 HTML 先用 `DOMParser` 解析到惰性文档，再按白名单一个节点一个节点重建 —— 脚本、`on*` 事件属性、`javascript:` 地址、iframe / 表单 / 媒体整棵丢掉。KaTeX 的输出和 mermaid 的 SVG 是库自己生成的（用户原文都经过转义 / 净化），前者走 innerHTML，后者 DOMParser 解析后只收 `<svg>` 节点。

### 11.4 语法支持现状

| 语法 | 表现 | 快捷键 |
|---|---|---|
| ATX `#`–`######`、Setext `===` / `---` | 六级字号，H1/H2 下边框；标记折叠；悬停行左侧浮出 `H2` 层级提示 | `Mod-0~6`、`Mod-=` / `Mod--` |
| `**` `__` 加粗、`*` `_` 斜体、`~~` / `~` 删除线、`<u>` 下划线、`==高亮==` | 高亮是荧光笔式只涂下半截；单个 `~` 按 GitHub 规矩也是删除线 | `Mod-b` / `Mod-i` / `Alt-Shift-5` / `Mod-u` / — |
| `<sub>` 下标、`^sup^` / `<sup>` 上标 | 缩小 + 上下偏移，标记折叠（`~x~` 不再是下标） | |
| 行内 HTML：`<b> <i> <s> <u> <mark> <sub> <sup> <kbd> <code> <small> <big> <span style> <font color size> <a href> <abbr title>` | 配对后映射成和 Markdown 同一套样式类，标签像 `**` 一样折叠；`style` 只放行 color / background / font-size / font-weight / font-style / text-decoration / text-align；`<a>` 的地址挂在 `data-href` 上，Mod + 点击打开；没配上对、不在白名单的标签原样淡色显示 | |
| `<br>`、`<img src alt width height>`、`<!-- 注释 -->` | `<br>` 真的换行（前面留 `↵`）；`<img>` 和 Markdown 图片同一个 widget，`javascript:` / 非图片 data: 地址拒绝；注释收成一个「注释」小标记，光标到那一行才露出 | |
| 块级 HTML（`<table>` `<h1>`–`<h6>` `<details>` `<div>` `<ul>` `<blockquote>` `<pre>` `<center>`…） | 光标不在块内时整块换成净化重建后的真实 DOM（表格、标题、折叠面板都能用，`<details>` 可开合，`<a>` 可点）；光标进去回到淡色等宽源码 | |
| `` `code` ``、``` ``` ``` / `~~~` 围栏、4 空格缩进块 | 围栏行折叠成上下封口，语言名 + **复制按钮**在右上；**按语言语法高亮**；缩进块只有底色 | `Shift-Mod-\`` / `Mod-Alt-c` |
| `[text](url "title")`、`[text][ref]`、`[ref]`（有定义）、`<autolink>`、裸 URL、`[ref]: url` | 链接色，URL / 标题 / 标签隐藏；悬停 tooltip 显示地址；**Mod + 点击打开**（桌面端走 opener 插件，只放行 http(s) / mailto / tel）；按住 Mod 变手形 | `Shift-Mod-k` |
| `[[标题]]`、`[[标题\|别名]]`、`[[标题#小节]]`、`[[标题^块]]` | 淡底 + 虚线下划的双链样式，括号折叠，别名形式只显示别名；**Mod + 点击**按标题跳到那篇笔记（含归档），带 `#小节` 的到了以后再滚到那个标题（`useApp.pendingAnchor`，目标笔记的目录算出来后由 DocumentView 消费）；`^块` 只识别不定位 | |
| `[^id]` / `[^id]: 内容` | 上标圆形徽章 / 定义行开头同款徽章，定义行缩小变灰；引用徽章悬停显示脚注内容，点击跳到定义，定义徽章点击跳回第一处引用。定义行按行识别，所以 `[^1]: 多个 词` 也认（以前只有单个词才会被解析成引用定义） | |
| `![alt](src "title")`、`![alt][ref]`、`![alt\|300]`、`![alt\|300x200]` | `<img>` widget，加载完淡入；title 进 tooltip，`\|宽` / `\|宽x高` 是 Obsidian 式尺寸 | `Shift-Mod-i` |
| `-` `+` `*` 无序、`1.` `1)` 有序、`- [ ]` 任务 | 无序按嵌套深度 `•` `◦` `▪` 轮换；有序**按位置自动编号**（源码 `1. 1. 1.` 显示 1 2 3，起始编号和 `.` / `)` 跟第一项走，嵌套列表各自计数）；勾选框可点击，勾上时盒子弹一下 + 对勾描边画出 | `Shift-Mod-]` / `Shift-Mod-[` |
| `>` 引用（可嵌套） | 按层级叠竖线、逐级缩进（最多 4 层） | `Shift-Mod-q` |
| `> [!NOTE]` / `[!TIP]` / `[!IMPORTANT]` / `[!WARNING]` / `[!CAUTION]`、`> [!任意中文标签]`、`> [!标签] 自定义标题`、`> [!标签]-` / `[!标签]+` | callout：首行换成带图标的徽章；五种语义标签各自配色 + 极淡底色；中文自定义标签走原型的样子（主色竖线、正文放大加重）；中文别名（注意 / 提示 / 重要…）也映射到语义色。标题跟在徽章后面加粗显示；`-` 默认收起（正文折成一行「… 展开 N 行」）、`+` 默认展开，徽章带一个 chevron，点徽章或那一行就把源码里的 `-` / `+` 翻过来 —— 折叠状态就存在源码里，不另外记 | |
| `---` 分隔线、GFM 表格 | 整行换成渐隐两端的细线；光标不在表内时换成 `<table>` widget，**单元格支持行内格式**（含行内 HTML 和 `<br>` 多行单元格），行悬停高亮，点击跳回源码 | — / `Mod-t` |
| ```` ```csv ```` / ```` ```tsv ```` 围栏 | 光标不在时整块换成表格 widget（第一行是表头，RFC 4180 引号规则），进去是普通代码块 | |
| `$x$`、`$$x$$`、独立的 `$$ … $$` 块 | KaTeX 渲染（首次出现公式时才拉 KaTeX 分包，拉到前显示等宽 TeX 源）；`$5 和 $10` 这类价格不算公式（开头 `$` 后不能是空白，结尾 `$` 前不能是空白、后不能是数字）；无法渲染时红色波浪线 + 源码 | |
| ```` ```mermaid ```` 围栏 | 光标不在时整块换成 SVG 图表（首次出现才拉 mermaid 分包，`securityLevel: strict`），语法错误把第一行错误信息显示在原位；暗色主题用 CSS 反色 | |
| 定义列表：`术语` 下一行 `: 释义`（可多条，中间可隔一个空行） | 术语加粗，释义缩进、`: ` 折叠；上一行长得像标题 / 列表 / 引用 / 代码围栏的不当术语 | |
| 缩写：`*[HTML]: HyperText Markup Language` | 正文里整词出现的 `HTML` 点状下划线 + 悬停全称（定义行自己除外，`HTML5` 这种不算）；定义行缩小变灰 | |
| 智能标点：`--` `---` `...` `(c)` `(r)` `(tm)` | 只在显示层换成 – — … © ® ™，**源码不动**；代码、公式、HTML、链接地址、表格、整行都是分隔符的行（分隔线 / Setext 下划线）不碰 | |
| `[TOC]` | 换成目录 widget：按层级缩进的标题列表，点击跳转；点空白处回到源码 | |
| 开头的 `---` YAML front matter | 折叠成「属性」块：围栏行换成封口，`key: value` 行等宽变灰；不再被当成分隔线 + Setext 标题 | |
| `:smile:` Emoji 短码、`&mdash;` 实体 | 替换成真实字符（短码表约 400 条，实体表常用 74 条，查不到保持原文），点击回源码 | |
| `:otw_fire:` 动态表情（§11.4.2） | 换成自己画的 SVG 动画表情，看得见就一直循环；像一个字符：光标贴着它不展开、退格整删、方向键一步跨过；单击放光标 + 从头再来，双击展开源码 | `Mod-e` 选择器 |
| 行尾两空格 / `\` 硬换行 | 显示一个极淡的 `↵` | |
| `\*` 转义 | 只藏反斜杠 | |
| 图片地址 | 网络地址、`data:image/` 原样；桌面端把本机绝对路径（`C:\…`、`/…`、`file://`）转成 asset 协议（`tauri.conf.json` 开了 `assetProtocol`，scope 只放行常见图片扩展名、大小写不限，Unix 上点开头的目录不放行；Cargo 开了 `tauri/protocol-asset`）；CSP 的 `img-src` 放行了 `https:` / `http:`，以前网络图片在桌面端根本加载不出来 | |

另有 `Mod-\` 清除格式（不删空行）、`Mod-]` / `Mod-[` 缩进、`Mod-l` 选行、`Mod-f` 查找 / 替换面板（`searchPanel.ts`：固定在窗口顶上标题栏那条带里、贴着文档区右边，不占文档流也不压正文，边打边搜并显示「第几个 / 共几个」，大小写 / 全词 / 正则开关也可以用 Alt+C / W / R，替换折在第二行；回车下一个、Shift+回车上一个，匹配项不在视口里时滚到正中；层级高过自绘标题栏，点它不会变成拖窗口）、`Mod-e` 动态表情选择器（mac 上是 ⌘E，Ctrl-E 仍是 Emacs 式行尾）。`Mod-k` 留给全局命令面板。

**❌ 仍不支持**：标题 `{#id}`、嵌入 `![[笔记]]`、`[[笔记^块]]` 的块定位、表格 widget 内直接编辑单元格、相对路径的图片（没有基准目录可言）、Obsidian 式的 callout 嵌套折叠。

### 11.4.1 动效

编辑区里的动画只动 opacity / transform / stroke，从不改尺寸（CodeMirror 靠 `getBoundingClientRect` 量高度）；减少动效由全局规则统一收掉。

| 时机 | 动画 |
|---|---|
| 光标进入语法范围、标记重新出现 | 标记 160ms 淡入 |
| 光标离开、替身出现（列表符号、Emoji、徽章、封口、分隔线） | 140ms 淡入 |
| 表格 / 目录 widget 出现 | 淡入 + 向上 3px 落定（220ms） |
| 勾选任务 | 盒子 `scale 0.82 → 1.1 → 1` 弹一下，对勾 `stroke-dashoffset` 描边画出；悬停亮边 + 光晕，按下微缩 |
| 复制代码 | 按钮平时半透明，悬停封口时实起来；复制成功图标切成对勾描边 + 「已复制」1.4s |
| 图片 | 加载完成后 320ms 淡入，失败时显示 alt |
| 标题 | 悬停时左侧 `H2` 提示淡入并右移 4px 归位 |
| 链接 / 双链 / 目录条目 | 下划线色、底色、位移的 140ms 过渡 |
| 动态表情 | 露出视口就一直循环（一轮动作 + 700ms 停顿），滚出去停回静止帧；同时循环最多 48 个（见下）；单击从头再来；从选择器插入时先整个弹出来再做动作 |

### 11.4.2 动态表情

**为什么是 SVG + Web Animations，不是 GIF / Lottie。** GIF 只有 1 位透明（暗色主题下带白边）、256 色、位图在高分屏发虚、JS 控制不了播放。这套表情是自己画的线稿，SVG 就能表达：矢量、颜色走 CSS 变量跟主题变、能精确控制「看得见就循环 / 滚出去就停 / 减少动效时停在静止帧」，不用引 WASM、不用改 CSP。以后要接第三方的彩色动画表情再上 Lottie。

**源文。** Markdown 里只是短码 `:otw_<id>:`（lezer 的 Emoji 语法只认字母数字下划线，所以前缀用下划线）。别的编辑器打开看到短码；列表摘要、目录、表格单元格的 `textContent` 里换成对应的 Unicode（`lib/emojiText.ts`，和设计稿一一对应，单测守着）。

**设计规矩**（`animatedEmojiSet.ts` 文件头，`animatedEmoji.test.ts` 逐条守着）：24×24 画布，1.7 圆头描边 + 同色系淡彩（淡彩 = 色相 22% 混进纸色，暗色下自动变深）；要动的部件带 `data-a`，身上不许写 `transform` 属性；只动 transform / opacity / stroke-dashoffset；每段动画的最后一帧等于静止态（只在视觉上无缝的对称旋转可以例外，且要声明 `seamless`）；平时隐藏的粒子（`fx`）最后一帧必须回到透明；一轮不超过 2.5 秒。

**排版。** 外框只占一个汉字的位置（1em 高、落在基线下 0.12em），图画成 1.3em 绝对定位压在上面 —— 多出来的只是视觉溢出，行盒子永远不会被撑高。以前按 1.3em 做外框时，正文行高 1.4 的行会多 0.5px、标题多 4px，CodeMirror 的高度图就歪了（e2e 逐行比对过带 / 不带表情的行高）。

**编辑器行为。**
- `eq` 只比短码不比位置：在它前面打字时复用 DOM，动画不会从头再来
- 选区规则 `selectionEntersRange`：光标严格在短码内部、或选区只切到一部分时才展开源码；贴在两边、整个被选中都保持表情
- 插入时，前面紧挨着 `:词`（`12:30`）或 `]`（行首 `[标签]:` 是引用定义）先垫一个空格
- 一直循环，但只在看得见的时候：全局一个 `IntersectionObserver`（上下各多看 64px），露出来开始循环、滚出去 `stop()` 回到静止帧。自动开始的随机晚 0–700ms（停在静止帧上等），同屏同时露出来的一排不会齐步走
- 循环怎么省：每轮用一个计时器从头播一遍（各部件自己的时长、delay、重复次数），`fill` 只有 `backwards`。做完的部件回到底下的静止样式（等于最后一帧，单测守着），结束了的动画不再每帧参与样式计算 —— 设计稿里部件平均约 1/3 的时间是停着的（等 delay、等别的部件、两轮之间的停顿），这段时间不占帧。试过把「一轮 + 停顿」折成一条 `iterations: Infinity` 的动画，停着的时候照样每帧重算：无头 Chromium（软件渲染）同屏 39 个表情时每帧重算样式的元素 301 → 186，主线程占用 46% → 39%。也试过把动画烘进静止帧那张图里（CSS `@keyframes`，同款共用一张图），结果每个实例每帧都要重绘整张 SVG 图，绘制反而从 8% 涨到 26%，放弃
- 同时循环的上限 48 个，超出的停在静止帧排队，前面有滚出去的就依次补上；悬停排队中的、单击、刚插入的不受限。无头 Chromium 上 308 个表情铺满一屏：上限 80 时 p95 帧间隔 33ms，48 时稳定 60 帧（主线程 55%）；「减少动效」随时拨动都会跟着停 / 恢复
- **静止时是一张图**：替身的成本几乎全在浏览器给新 SVG 算样式（一个表情约 10 个元素，和 `color-mix` / `var()` 无关，只和元素数量有关）。静止帧改成 `<svg viewBox="0 0 24 24"><image href="data:…"/></svg>`：同一份 SVG 连同当前主题解析好的颜色（`--ae-*`、画布、墨色）和 `EMOJI_PART_CSS` 序列化成 data: 图，每个表情每套主题生成一次，所有实例共用；只在循环时把活的 SVG 叠上去（静止帧 `display: none`，不卸载），停下时摘掉。切换主题时 `MutationObserver` 盯着 `data-theme`，把页面上所有静止帧换成新颜色。拿不到主题颜色（样式表未加载、测试环境）时退回活的 SVG
- 为什么是 `<image>` 不是 `<img>`：`<img>` 的落点吸附到整设备像素，行内 SVG 按亚像素绘制，表情在一行字里的位置几乎总是小数 —— 用 `<img>` 时静止帧和动起来的第一帧差半个像素，一播就「抖」。外框也必须保持原来的尺寸和 viewBox（1.3em、`0 0 24 24`，图自己的画布扩到 `-3 -3 30 30` 放在 (-3, -3)）：换成数学上等价的更大外框，像素吸附会让整个表情挪一个设备像素
- 画质验证（新旧两版并排逐像素比较，当时的 24 个表情 × 静止 + 8 帧，亮暗两套；之后新增的表情同样核对了静止帧图片和活 SVG 的一致性）：所有动画帧逐像素一致（最大差 0）；静止帧与原来的矢量渲染最大差 8–12/255，只在抗锯齿边缘，任何一个像素都不超过 16/255（正文 17px 1x / 2x、H1 38px、H2 30px 都测过）
- 成本（无头 Chromium 实测，优化前 → 后）：一屏 400 个表情首次渲染 217ms → 103ms（样式重算 150 → 49ms）；一屏 30 个 11.4 → 9.0ms；每个表情的 DOM 元素 10.5 → 5.4（其中 2.4 个是 CodeMirror 给行内 widget 加的占位）；400 个在屏时打字中位数 4.4 → 3.8ms；连续滚动 2000 个表情的长文档，超过 20ms 的帧 16–29 → 12–16，样式重算 760 → 650ms（关掉动效时 510 → 300ms）

**选择器（`Mod-e`）。** 贴着光标弹出，下面放不下翻到上面、左右夹在窗口里；尺寸用 `offsetWidth/Height` 量（入场的 `scale(0.96)` 这时已经挂上，量包围盒会小 4%）。最上面一行是最近用过的（`localStorage.otw.emoji.recent`，读写都兜住异常），下面六组（心情、干劲、在路上、日常、打工人、吃瓜）各一行；网格限高 322px 滚动，键盘走到看不见的格子时滚过去（只在键盘移动时滚，悬停不滚，免得边上的格子一碰就跳）；上下都放不下完整面板时（矮窗口、光标在中间），挑空间大的一侧、把网格压矮到正好放得下（`placePicker`，最少留 120px）；方向键按行列走、回车插入、Esc 关闭并把焦点和原选区还给编辑器；输入框里打字就是搜索（中文、近义词、拼音、英文、Unicode 本身都行），输入法组字时的回车不算。当前格循环播放，其余静止（一屏几十个在同一个小面板里一起动太吵）。两个 CodeMirror 的坑：
- 光标带折行方向（`assoc`，按 End 或点在折行处）时，编辑器一失焦 CodeMirror 就会在下一次测量里 `enforceCursorAssoc`，改写 DOM 选区顺手把焦点抢回正文。打开前先把方向清掉，Esc 关闭时还原
- 打开走 `requestMeasure`：坐标在读阶段量，并且排在上一个按键留下的测量之后
退场 140ms，退场中不接点击、不再监听外部事件（否则会把紧接着新开的选择器关掉）；每次打开换一个 key，上一个没退完时再按快捷键得到的是全新的实例。

### 11.5 编辑器与动画的关系

CodeMirror 靠 `getBoundingClientRect` 定位光标和测量 widget 高度，两条硬规则：

1. **祖先不得跑 layout / transform 动画时挂载**。工作区进场因此是纯 opacity；专注模式的动画在 chrome 层而不是正文层
2. **块级 widget 的留白必须在边框盒里（padding），不能用 margin** —— margin 不在 `getBoundingClientRect` 里，高度图比真实布局短，点击会定位到错误的行。`CodeFenceWidget`、`TableWidget`、`HorizontalRuleWidget`、`ImageWidget` 都遵守；`widgetSpacing.test.ts` 用正则守着 CSS
3. **光标进出时，折起来的替身和露出来的源码行一样高** —— 否则点进一个块，它上面那一行一变高矮，整块内容连同刚点的那一行一起跳。代码块 / 属性块的围栏封口和 ``` / `---` 源码行共用一组尺寸变量（`--fence-*`、`--frontmatter-*`），源码行用 `::before` 画出同样的圆角框（以前源码行比开头封口矮 15px、比结尾封口高 3px，框也露着口）；分隔线露出的 `---` 行高等于分隔线；整行注释的小标记等于一行源码；等宽的源码标记（短码、实体、脚注、callout 标签、行内 HTML、行内公式）`line-height: 1`，不再把所在行撑高 0.6–1px。逐行比对（光标放到「语法全览」每一行，量周围 ±10 行的高度）：会改变别的行高度的从 211 行降到 25 行，剩下的是 Setext 下划线露出、折叠的 callout 展开（都是设计如此），以及 [TOC] 和几张在无网络的测试环境里加载失败的外链图片
4. **正文区的鼠标指针一律是 I 形**，替身（列表符号、表格、代码块封口、HTML 块、图表、目录、动态表情）和块之间的留白也是 —— 以前替身是箭头、表情是手形，滚动时内容从静止的鼠标底下滑过，指针来回跳，看着一闪一闪。只有真正能点的控件（勾选框、复制按钮、目录条目、折叠按钮、脚注徽章）是手形；通用规则用 `:where()` 压到最低优先级，不盖它们

### 11.6 持久化

- `createDebouncedSaver(save, 400)`：串行写链；保存中继续输入不会覆盖正在写的版本；失败版本放回队列，下次 flush 重试
- 外部回填用 `externalSync` annotation 标记，不触发保存；**只在本地没有未同步编辑时回填**（P0：某天备注异步到达时不能覆盖用户已输入的内容）
- 目录树通过 `EditorOutlineHandle` 直接用行号 `scrollIntoView`，不依赖 DOM 锚点
- 中文 IME：`spellcheck="false"`；CodeMirror 自身处理 composition，没有额外门控

### 11.7 占位预览（`lib/markdown.tsx::renderMarkdown`）

只在 `doc.editor` 为空（数据未到）或空状态文案时渲染，返回真实 React 节点（无 `dangerouslySetInnerHTML`）。它的语法覆盖比编辑器窄（无 Setext、无图片、无 `<u>`、无引用链接），但因为几乎只渲染空串，实际不影响体验。`buildOutline`（ATX / Setext / callout 标签）是真正在用的部分；摘要和字数在 `lib/plainText.ts`。

---

## 12. 各模块现状

### 12.1 笔记

- 列表：标题本地筛选（不走全文搜索，见 §6.3）、三种排序（更新 / 创建 / 标题，单选菜单）、置顶分组、选中高亮 `layoutId` 滑动、`+` 新建（等后端分配 id）
- 行菜单 `RowMenu`：置顶 / 归档 / 删除（Radix DropdownMenu + Motion）
- 正文：编辑器；标题可编辑；右下删除；专注模式入口
- 双链 `[[标题]]`：保存时后端同步 `link(kind='ref')`；**编辑器内无特殊表现，也没有点击跳转**

### 12.2 今日TODO

就是今天这一天的 `day_doc`（§7.3）。标题可编辑，空标题显示占位「无标题笔记」。状态栏会写明「延续自 9月14日，还没有今天自己的记录」或「今天的内容会延续到明天，直到你改动它」。

### 12.3 /GOAL

一个周期一篇。分段「周 / 月 / 年」；标题由 `goalTitle(horizon, periodStart)` 算出，不可编辑；没写过的周期是空白编辑器，第一次保存才建行。状态栏：周期范围 + 字数 + 上次更新。

### 12.4 日历

- 月历面板：固定 6×7 网格、ISO 周数列、每周自成一个 grid 行（整行高亮用 `inset-0`）、方向感知切月、「今天」按钮只在离开今天时出现
- 两套颜色语义：主色 = 时间光标（今天、选中圈）；墨色 = 有没有写（`markedDates` 的日子数字加深 + 墨点）
- 右侧分段「日TODO / 周·月·年 GOAL」：日 = 选中那天的 `day_doc` + 「当日安排」（`task.due_date` 当天的任务，`ActionItem` 勾选走 `task_toggle`）；周月年 = 选中日期所在周期的 GOAL
- `calendarMarked` 一次取 2000–2100 全范围，保存后本地增删

### 12.5 归档

列表按 `archived_at` 降序；分类 · 日期；悬停旋转的恢复按钮；正文带「已归档 · 日期」横幅，仍可编辑。

### 12.6 扩展

整页视图，不走 `DocumentView`。`extensions/catalog.ts` 定义 `ExtensionManifest`（schemaVersion 1、id 正则、≤2MB 包）和 `ExtensionCatalogProvider` 接口；当前实现 `localExtensionCatalog`：官方占位条目（数学公式、Mermaid、增强 Callout…，全部 `downloads: 0`）+ 本地导入的清单存 localStorage。**没有扩展运行时**，「下载」不做任何事。接口留给以后的审核服务 / CDN。

### 12.7 命令面板

`Mod-k`，自绘浮层（Motion `AnimatePresence`，非 Radix）。三组：跳转（六个工作区）、笔记（本地按标题 / 摘要过滤）、外观（亮 / 暗 / 跟随系统 / 减少动效）。方向键 + 回车；捕获阶段处理 Esc 并 `preventDefault`，专注模式的 Esc 在冒泡阶段看 `defaultPrevented`，两者不打架。

### 12.8 提醒卡片（已移除）

原型右下角有一张提醒卡片，文案是写死的两套（笔记 / GOAL），背后没有任何提醒调度，还压着目录树的底部，已经删掉。要做提醒得先有调度，见 §16。

---

## 13. 性能

### 13.1 目标（未变）

冷启动可交互 < 800ms；视图切换首帧 < 100ms；稳定 60fps；主线程无 > 50ms 长任务；常驻内存 < 150MB；安装包 < 20MB。

### 13.2 已落地的手段

| 手段 | 位置 | 效果 |
|---|---|---|
| 编辑器分包 + bootstrap 预载 | `DocumentView` / `main.tsx` | 首帧直接是编辑器，消掉「预览 → 编辑器」的整篇重排闪动（实测原来 1162ms 会闪一次） |
| 装饰拆两层 | `MarkdownEditor` | 53KB 文档单次装饰 5.3ms → 0.57ms |
| 目录树 250ms 防抖 + 只在标题集合变化时 setState | `MarkdownEditor` / `DocumentView` | 长文档下不再每键重扫 |
| `DocumentPreview` 做成 `memo` 组件 | `DocumentView` | 编辑器已挂上时不执行（90KB 文档约 10ms） |
| 工作区进场不带 transform；列表栏只动 transform、主内容 popLayout 冻结退场屏 | `Shell` | 布局只变一次，避免 lineWrapping 逐帧折行 |
| 字重不做 CSS 过渡 | `Sidebar` / `Segmented` / `Outline` | 中文每个小数字重都要重新匹配回退字体，实测一个标签一帧 11ms（拉丁文 0.5ms），过渡 140ms 就是 140ms 掉帧 |
| 大块头 widget（表格 / 图表 / 公式 / HTML 块 / 目录）的身份里不含文档位置，点击时用 `posAtDOM` 现算 | `widgets.ts` / `diagram.ts` / `math.ts` | 位置进了 `eq`，在上方打一个字后面所有替身都重建：mermaid 重跑、KaTeX 重排，一个字 50ms；改后 3ms。图表 SVG 另按代码缓存 40 张 |
| `inlinePlainText` 带缓存 | `inlineDom.ts` | 块级层每次重建都给全文标题和脚注定义算纯文本，长文档里上百次 |
| 57KB / 1700 行全语法样例（`docs/语法全览.md`）实测 | | 挂载 126ms；按键 2.4–3.9ms（p95 ≤ 5.8）；滚轮步进 0.1ms（p95 5）；整页跳转 9ms（最大 15） |
| `OverlayScrollbar` 指数缓动跟随 | | `scrollHeight` 边滚边涨时滑块不走楼梯 |
| jieba 后台预热；派生列写入时算 | Rust | 首次搜索 / 列表不付分词代价 |
| Rust release：`opt-level = "s"`、LTO、`codegen-units = 1`、`panic = "abort"`、strip | `Cargo.toml` | |
| Vite `manualChunks`：`motion`、`vendor`（react） | `vite.config.ts` | 编辑器由动态 import 自动成块 |

未做：长列表虚拟化（数据量还用不上）、Web Worker、`content-visibility`、性能回归 CI。

---

## 14. 构建与发布

### 14.1 `tauri.conf.json` 要点

- 窗口：1440×900（最小 940×620），`decorations: false`，`visible: false`，`backgroundColor: #F1F1ED`（和 rail 同色，show 前不闪白）
- CSP：`default-src 'self'; img-src 'self' asset: http://asset.localhost data:; style-src 'self' 'unsafe-inline'; connect-src 'self' ipc: http://ipc.localhost`
- 打包目标：`nsis`（`installMode: currentUser`，中英文）、`dmg`（macOS ≥ 13）、`appimage`、`deb`；WebView2 用 `downloadBootstrapper`
- capabilities：`core:default`、`core:window:allow-start-dragging`、`opener:default`、`window-state:default`

### 14.2 插件

`single-instance`（二次启动拉回已有窗口）、`window-state`（记忆位置大小）、`opener`。**没有** updater、notification、global-shortcut、autostart、dialog。

### 14.3 未做

自动更新、代码签名、每日自动备份（只有迁移前备份）、导出 Markdown 文件夹。

---

## 15. 测试

| 层级 | 工具 | 现状 |
|---|---|---|
| Rust 单元测试 | `cargo test` + `:memory:` SQLite（`db::test_support`） | **59 个**，全绿：迁移链路（含 0003 的行动项折回正文）、FTS5 可用、外键生效、笔记 CRUD / 搜索 / 双链、目标周期校验与延续语义、任务勾选与 activity、rrule（跨年、BYSETPOS、EXDATE + override、DST 固定本地时间、fold / gap）、分词、种子幂等 |
| 前端单元 | Vitest（node）| 命令层 `markdownCommands`、注册表、表格解析、防抖保存器、store 保存判重、`lib/markdown` |
| 前端 DOM | Vitest + `happy-dom`（文件头 `// @vitest-environment happy-dom`） | `MarkdownEditor.dom.test.ts`：装饰在真实 EditorView 上的行为 |
| CSS 守卫 | Vitest 读 `globals.css` | `widgetSpacing.test.ts`：块级 widget 只能用 padding 留白 |
| E2E / 性能回归 | — | 未做 |

前端 **9 个文件 / 71 个用例**，全绿。

```bash
pnpm test
```

```bash
cd src-tauri; cargo test --no-default-features --lib
```

> `--no-default-features` 时 `main.rs` 编不过（`run()` 被 feature 门控掉了），所以要加 `--lib`。带默认 feature 的 `cargo test` 需要完整的 tauri 构建环境，更慢。

---

## 16. 尚未实现 / 已实现未接入

| 项 | 状态 | 位置 |
|---|---|---|
| 全文搜索入口 | 后端、`Backend`、store 全在；**界面没有调用** | `NotesView` 改成标题筛选 |
| 双链的反向链接面板 | 保存时写 link 表，编辑器内已能显示并 Mod + 点击跳转；**没有读侧**（谁引用了我） | `note::sync_wiki_links` |
| 日历事件（`event` / `calendar` / `event_override`）| 表 + `expand_rrule` + 时区转换 + 单测齐全；**没有命令，UI 只用 `task.due_date`** | `domain/event.rs` |
| 复盘（`review` / `activity` 读侧）| `activity` 一直在写；没有聚合、没有视图 | `domain/activity.rs` |
| KR / 进度 / 标签（`key_result` / `tag` / `entity_tag`）| 只有表 | `0001_init.sql` |
| `db_stats` | 命令在；没有设置页展示它 | |
| 提醒调度、通知 | 无（原型里那张静态提醒卡片已删掉） | |
| 扩展运行时 | 只有目录与导入 | §12.6 |
| 设置界面 | 偏好只能从命令面板改 | |
| 数学公式 / Mermaid | 无；扩展目录里是占位项 | §11.4 |
| dnd-kit / fractional-indexing / marked / Radix dialog·popover·tooltip | 已安装未使用 | `package.json` |

---

## 17. 风险登记

| # | 风险 | 影响 | 概率 | 应对 |
|---|---|---|---|---|
| 1 | Linux WebKitGTK 动画卡顿 | Linux 体验差 | 高 | 明确二等公民；必要时检测 WebKitGTK 降级动效 |
| 2 | CodeMirror 装饰与 IME 的边角（组合中光标进入标记范围） | 吞字 / 跳位 | 低 | CodeMirror 自身处理 composition；发现问题在 `MarkdownEditor.dom.test.ts` 加用例 |
| 3 | jieba 分词质量（专业术语、人名） | 搜不到 | 中 | 支持自定义词典；提供精确匹配 |
| 4 | 表结构里大量「建了没用」的表 | 新人误以为已实现；将来改动要迁移 | 中 | §16 列清楚；动这些表前先看是否直接删 |
| 5 | tauri-specta RC 版本 | 升级可能 break | 低 | 三个版本号 `=` 锁死 |
| 6 | 前端把每篇笔记全文都拉进内存 | 上千篇后启动慢 | 低 | 到时改成列表用 summary、切换时再 `noteGet` |
| 7 | 范围蔓延 | 发不出 v1 | 高 | §1.3 与 `TODO.md` 的 P7 清单不松口 |
| 8 | macOS 无法跑 E2E | 回归靠手测 | 中 | Rust 层覆盖率拉高 |

---

## 附录 A：常用命令

浏览器调 UI（mock 后端）：

```bash
pnpm dev
```

桌面端：

```bash
pnpm tauri dev
```

类型检查 / lint / 前端测试：

```bash
npx tsc --noEmit
```

```bash
pnpm lint
```

```bash
pnpm test
```

Rust 测试与绑定导出（不起 app）：

```bash
cd src-tauri; cargo test --no-default-features --lib
```

```bash
cd src-tauri; cargo run --example export_bindings --features typegen --no-default-features
```

## 附录 B：Vite 配置要点

```ts
// vite.config.ts
export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: { alias: { "@": path.resolve(__dirname, "./src") } },
  clearScreen: false,
  server: { port: 1420, strictPort: true, watch: { ignored: ["**/src-tauri/**"] } },
  build: {
    target: ["es2022", "chrome110", "safari16"],   // WebView2 与 WKWebView 的交集
    minify: "esbuild",
    sourcemap: !!process.env.TAURI_ENV_DEBUG,
    rollupOptions: { output: { manualChunks: { motion: ["motion"], vendor: ["react", "react-dom"] } } },
  },
});
```

---

*本文档描述的是代码现状。改了架构性的东西（新命令、新表、新 store、动效规则的例外）请同步更新对应小节。*
